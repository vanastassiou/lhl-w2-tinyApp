<!DOCTYPE html>
<html lang="en">
<body>
  <header>
      <% include partials/_header %>
  </header>
  <h1>Check out this sweet TinyURL clone</h1>
  <p>(We definitely need more of these.)</p>
  <ul>Here's what we're keeping track of for you:
    <% for (var key in urls) { %>
        <li><%= key %> - <%= urls[key] %>
          <a href="/urls/<%= key %>">Edit</a>
          <form method="POST" action="/urls/<%= urls[key] %>/delete">
            <button type="submit">Delete</button>
          </form>
        </li>
    <% }; %>
  </ul>
</body>
</html>
